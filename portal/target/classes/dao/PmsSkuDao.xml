<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunnice.portal.dao.PmsSkuDao">
    <select id="queryCount" resultType="Integer" parameterType="java.lang.Long">
        SELECT count(*) FROM pms_sku_stock WHERE product_id=#{id}
    </select>
</mapper>